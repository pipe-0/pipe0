---
title: Run pipes (sync)
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Enrich data synchronously. This endpoint waits for the enrichment to
          complete before returning a result. Use with fewer than 10 records.
          To enrich more data at once use the '/run' endpoint.
---

Enrich data synchronously. This endpoint waits for the enrichment to complete before returning a result. Use with fewer than 10 records. To enrich more data at once use the `/run` endpoint.

<APIPage document={"https://api.pipe0.com/openapi"} operations={[{"path":"/v1/pipes/run/sync","method":"post"}]} />
