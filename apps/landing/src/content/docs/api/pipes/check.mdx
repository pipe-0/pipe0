---
title: Check pipes run
description: Check the status and collect the results of an asynchronous pipe run via the pipe0 API.
full: true
_openapi:
  preload:
    - https://api.pipe0.com/openapi
  method: GET
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Check status of enrichment task. Poll this endpoint until the
          status becomes 'completed' or 'failed'. Recommended polling intervals
          are between 1-3s.
---

Check status of an enrichment task. Poll this endpoint until the status becomes `completed` or `failed`. Recommended polling intervals are between 1 and 3 seconds.

<APIPage document={"https://api.pipe0.com/openapi"} operations={[{"path":"/v1/pipes/check/{run_id}","method":"get"}]} />
