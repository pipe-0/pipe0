---
title: Overview
description: Search creates records from filters. Use it to find people and companies, often called prospecting, list building, or sourcing.
date: 2026-07-06
---

While pipes add fields to records you already have, search creates the records. Use it when you start from a filter ("software engineers in Berlin") instead of a list. The <AppLink linkType="searchCatalog">search catalog</AppLink> lists every dataset and its filters.

## Run a search

This request fetches a list of people from the [`people:profiles:crustdata@2`](/docs/search-catalog/people:profiles:crustdata/2) dataset. Each request takes a single search. `limit` sets the page size; use <AppLink linkType="pagination">pagination</AppLink> for more results.

export const searchPayload = {
  search: {
    search_id: "people:profiles:crustdata@2",
    config: {
      limit: 25,
      filters: {
        current_job_titles: { include: ["Software Engineer", "Developer"] },
      },
    },
  },
};

<RequestExample kind="search" payload={searchPayload} />

The response contains one result object per match, with the same field shape pipes use:

```json title="Response (truncated)"
{
  "id": "jyg5w5p7unhcufkp8f7z5za8",
  "status": "completed",
  "search_id": "people:profiles:crustdata@2",
  "total_pages": 2764,
  "next_page": {
    // pass this as `search` to fetch the next page
  },
  "results": [
    {
      "name": {
        "type": "string",
        "value": "Gayle Pouros",
        "status": "completed"
        // ...
      },
      "job_title": {
        "type": "string",
        "value": "Product Intranet Manager",
        "status": "completed"
        // ...
      }
    }
    // ...
  ]
}
```

The full response carries more metadata than shown here. See the [response object](/docs/search/response-object).

## From search to enrichment

The `results` array is a valid `input` for a pipes request: search for people, then hand the results to pipes to add emails or phone numbers. No reshaping needed, because results come back as <AppLink linkType="inputExpansion">expanded field values</AppLink>.

See the [Search then enrich](/docs/examples/search-then-enrich) example for the full pattern.

## Paginate

Every search response includes a `next_page` field. Pass it as the `search` payload to fetch the next page; when it is `null`, there are no more results. See <AppLink linkType="pagination">Pagination</AppLink> for a full iteration loop.

## Search in bulk

The sync endpoint above waits for the result. If a search takes longer than 3 minutes, the sync endpoint returns `408`; the search keeps running, and the error message carries the run id so you can still poll for the result. For large searches, create an async task with `POST /v1/search/run` (same payload), then poll `GET /v1/search/check/{run_id}` until `status` is `completed` or `failed`.

```typescript tab="TypeScript" title="Poll for the result"
const result = await fetch(`https://api.pipe0.com/v1/search/check/${runId}`, {
    method: "GET",
    headers: {
        "Authorization": `Bearer ${API_KEY}`
    }
});
```

```python tab="Python" title="Poll for the result"
import requests

headers = {
    "Authorization": f"Bearer {API_KEY}",
}
response = requests.get(f"https://api.pipe0.com/v1/search/check/{run_id}", headers=headers)
```

```bash tab="cURL" title="Poll for the result"
curl -X GET "https://api.pipe0.com/v1/search/check/$RUN_ID" \
-H "Authorization: Bearer $API_KEY"
```

Read more about the trade-offs in <AppLink linkType="async">sync vs async</AppLink>.

## Next steps

<Cards>
  <Card icon={<Search />} title="Search catalog" description="Every dataset with filters and credit prices." href="/docs/search-catalog" />
  <Card icon={<ArrowRightLeft />} title="Search then enrich" description="Pipe search results straight into an enrichment request." href="/docs/examples/search-then-enrich" />
  <Card icon={<Braces />} title="Request payload" description="Every property of a search request." href="/docs/search/request-payload" />
</Cards>
