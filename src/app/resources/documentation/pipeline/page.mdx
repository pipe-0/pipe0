---
title: pipe0 Documentation | Pipeline
category: Concepts
priority: 2
description: Learn about Pipelines. Customizable enrichment workflows.
icon: stack
date: 2025-03-07
isPublished: true
---

# Pipeline

<AppLink linkType="pipeline">Pipeline</AppLink> is the data model we use to enrich data. It is extremely powerful.

A pipeline is made up of:

⛓️‍💥 Pipes and their order <br />
🔢 Fields added by your <AppLink linkType="inputObject">input</AppLink> <br />
🧮 Logic and conditions

## Validation

Before pipelines are used, they are checked for validity.

In a valid pipeline:

- All pipes have their input requirements met
- All logical conditions between pipes can be resolved

Only valid pipelines can be run. If you send an invalid pipeline to our servers, we will respond 
with a [400 Bad Request](https://developer.mozilla.org/de/docs/Web/HTTP/Reference/Status/400) status code instantly.

## Pipeline request

You can run a pipeline by sending a pipeline request. You can choose between <AppLink linkType="syncProcessing">synchonous processing</AppLink> 
(wait for the enrichment to complete) and <AppLink linkType="asyncProcessing">asynchonous processing</AppLink> (polling).

<CodeTabs items={["Typescript", "Python", "cURL"]}>
    <div>
```typescript
const result = await fetch("https://api.pipe0.com/v1/run", {
method: "POST",
headers: {
    "Authorization": `Bearer ${API_KEY}`,
},
body: JSON.stringify({
    pipes: [
    {
        pipe_id: "PeopleBusinessEmailWaterfallV1"
    },
    ],
    input: [
        {
			id: 1,
            name: "John Doe",
            company_name: "Google LLC"
        }
    ]
})
});
```
    </div>
    <div>
     ```python
import requests
import json

headers = {
    "Authorization": f"Bearer {API_KEY}",
}
payload = {
    "pipes": [
        {
            "pipe_id": "people:workemail:waterfall@1"
        },
    ],
    "input": [
        {
			"id": 123,
            "name": "John Doe",
            "companyName": "Google LLC"
        }
    ]
}
response = requests.post("https://api.pipe0.com/v1/run", headers=headers, json=payload)
```
    </div>
    <div>
    ```bash
curl -X POST "https://api.pipe0.com/v1/run" \
-H "Authorization: Bearer $API_KEY" \
-d '{
        "pipes": [
            {
            "pipe_id": "people:workemail:waterfall@1"
            },
        ],
        "input": [
            {
            "name": "John Doe",
            "company_name": "Google LLC"
            }
        ]
    }'
```
    </div>
</CodeTabs>

When your pipeline is run, input objects get converted to <AppLink linkType="record">output records</AppLink>.
You can see the shape of output records in the <AppLink linkType="pipelineResponse">pipeline response</AppLink> section.

### Order of pipes

You may spend time reading pipe documentation to make sure pipes are added in the correct order. This is not necessary.
We analyze pipelines and find the best order of execution.

As a user, the only thing you need to ensure is that the pipeline can be 
processed. For this, all pipes must have their input requirements met.

If your application runs in the browser or is based on Javascript you can use the <AppLink linkType="sdk">browser SDK</AppLink> to 
validate your pipeline before sending a request to our servers.

## Pipeline Response

All enrichment endpoints return the same response format: A <AppLink linkType="pipelineResponse">pipeline response</AppLink>:

- `POST https://api.pipe0.com/v1/run` (used to run the pipeline)
- `GET https://api.pipe0.com/v1/check` (used to check the processing state)
- `POST https://api.pipe0.com/v1/run/sync` (used to run the pipeline synchonously)



```JSON
{
  "id": "sgrqhch5qqinn58ojs192zne",
  "order": [
    "e99e5017-7943-4dc1-91f9-67f70a9fd6dd",
  ],
  "field_definitions": {
    "id": {
      "type": "string",
      "required": false,
      "label": "id",
      "added_by": {
        "ref": "input",
        "hash": null
      }
    },
    "email": {
      "type": "string",
      "required": false,
      "label": "Email",
      "added_by": {
        "ref": "input",
        "hash": null
      }
    },
    "company_website_url": {
      "type": "string",
      "required": true,
      "label": "Company Website URL",
      "added_by": {
        "ref": "email:url@1",
        "hash": "e0b8a4ef"
      }
    }
  },
  "pipes": [
    {
      "pipe_id": "email:url@1",
      "config": {
        "input_fields": {
          "email": {
            "alias": ""
          }
        },
        "output_fields": {
          "company_website_url": {
            "alias": ""
          }
        }
      }
    }
  ],
  "errors": [],
  "records": {
    "e99e5017-7943-4dc1-91f9-67f70a9fd6dd": {
      "id": "e99e5017-7943-4dc1-91f9-67f70a9fd6dd",
      "errors": [],
      "fields": {
        "id": {
          "value": "e99e5017-7943-4dc1-91f9-67f70a9fd6dd",
          "status": "completed",
          "type": "string",
          "reason": null,
          "claimed_by": {
            "ref": "input",
            "hash": null
          },
          "resolved_by": {
            "ref": "input",
            "hash": null
          },
          "meta": null,
          "ui": {
            "severity": "none"
          }
        },
        "email": {
          "value": "emma@amazon.com",
          "status": "completed",
          "type": "string",
          "reason": null,
          "claimed_by": {
            "ref": "input",
            "hash": null
          },
          "resolved_by": {
            "ref": "input",
            "hash": null
          },
          "meta": null,
          "ui": {
            "severity": "none"
          }
        },
        "company_website_url": {
          "value": "https://amazon.com",
          "status": "completed",
          "type": "string",
          "reason": null,
          "claimed_by": {
            "ref": "email:url@1",
            "hash": "e0b8a4ef"
          },
          "resolved_by": {
            "ref": "email:url@1",
            "hash": "e0b8a4ef"
          },
          "meta": null,
          "ui": {
            "severity": "none"
          }
        }
      }
    },
  },
  "status": "completed"
}
```

## Failure modes

If your pipeline is valid the endpoint will always return a HTTP 200 OK status.

The result of the processing can be read from the global task status as well as the field status
of each record field. Remember, just because your pipeline has passed validation does not mean 
that processing succeeded for every record. 
