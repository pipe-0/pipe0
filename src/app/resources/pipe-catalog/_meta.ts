import { MetaRecord } from "nextra";

export default {
  index: {
    title: "Pipe Catalog | Index",
  },
  "*": {
    theme: {
      sidebar: false,
      toc: false,
      layout: "full",
      pagination: false,
      collapsed: true,
    },
  },
} as MetaRecord;
